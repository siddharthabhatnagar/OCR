# Local dev env. Copy to .env and edit. Most settings have sensible defaults.
# On Vercel, set these as Project Environment Variables.

# Auth (optional). If set, clients must send `Authorization: Bearer <key>`.
# NOTE: Currently informational only — auth not enforced in this version.
API_KEY=
