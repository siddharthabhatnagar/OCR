# Docling OCR dependencies
# Docling is IBM's document AI library — pure CPU, lightweight, excellent for RAG.
# https://github.com/DS4SD/docling

# Core Docling package (pulls in torch CPU, transformers, etc.)
docling==2.7.0

# Web framework for the ASGI app (Vercel Python runtime expects `app` variable)
starlette==0.41.3
python-multipart==0.0.20

# PDF/image processing
PyMuPDF==1.24.14
Pillow==11.0.0

# Utilities
pydantic==2.10.4
loguru==0.7.3

# NOTE: Vercel has a 250MB unzipped bundle limit. Docling + its deps
# (torch CPU, transformers) come to ~220MB which fits.
# If you hit the limit, switch to PyMuPDF4LLM (Option 2 in README)
# which is ~30MB total.
